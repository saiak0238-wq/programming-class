void setup()
{
  pinMode(9, OUTPUT);
  Serial.begin(9600);
}

void loop()
{
  int analog = analogRead(A1);
  Serial.println(analog);
  
  int duty = 255.0/1023.0 + (float)analog ;

 analogWrite(9, duty) ;
}
