void setup()
{
  pinMode(8, OUTPUT);
  Serial.begin(9600);
}

void loop()
{
  int value = analogRead(A0);
  Serial.println(value);
  
  int servo = 500 + ((1023/200) * value) ;

  digitalWrite(8, 1);
  delayMicroseconds(servo);

  digitalWrite(8, 0);
  delayMicroseconds(20000 - servo);
}
