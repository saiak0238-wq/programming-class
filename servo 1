void setup()
{
  pinMode(9, OUTPUT);
  Serial.begin(9600);
}

void loop()
{
  int analog = analogRead(A1);
  Serial.println(analog);
  
  int duty = (float)255/(float)1023 + (float)analog ;

 analogWrite(9, duty) ;
}
